export interface BoardPoint {
  x: number;
  y: number;
}

export interface BoardMetrics {
  markPadding: number;
  circleRadius: number;
  markStroke: number;
  gridStroke: number;
  winStroke: number;
}
